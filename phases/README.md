# Canonical Phase Workspace

This directory stores phase-level evidence created under the current mastery-based roadmap.

Each phase can contain:

```text
README.md
notes/
exercises/
tests/
projects/
debugging/
```

Only verified current-cycle material belongs here.

Historical notes remain under `notes/` as reference.
