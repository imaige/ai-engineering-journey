# Tests

This directory stores non-sensitive mastery-test artifacts and review summaries.

A test result can contribute to a phase gate, but the canonical phase status is maintained in `PROGRESS.md`.
